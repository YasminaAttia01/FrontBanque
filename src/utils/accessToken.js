import React from "react";

let accessToken = "";

const getAccessToken = () => {
  return accessToken;
};

const setAccessToken = (token) => {
  accessToken = token;
};

const AccessTokenHandler = () => {
  // You can use the getAccessToken and setAccessToken functions here
  // and in other parts of your React application as needed.

  return <></>; // Return your JSX or component content here
};

export default AccessTokenHandler;
